<?php
declare(strict_types = 1);

namespace {{ projectName }}\Domain\Repository;

use {{ projectName }}\Domain\Entity\{{ entityName }};

/**
 * {{ entityName }} Repository interface.
 *
 * @category   {{ projectName }}
 * @package    Domain
 * @subpackage Repository
 */
interface {{ entityName }}RepositoryInterface
{
    /**
     * Execute the save into the database for the entity {{ entityName }}.
     * @param \stdClass $object
     * @return {{ entityName }}
     */
    public function execute(\stdClass $object): {{ entityName }};
}
