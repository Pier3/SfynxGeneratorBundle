<?php
declare(strict_types = 1);

namespace {{ projectName }}\PresentationBundle\DependencyInjection;

use Symfony\Component\Config\FileLocator;
use Symfony\Component\DependencyInjection\ContainerBuilder;
use Symfony\Component\DependencyInjection\Loader;
use Symfony\Component\HttpKernel\DependencyInjection\Extension;

/**
 * This is the class that loads and manages your bundle configuration
 *
 * To learn more see {@link http://symfony.com/doc/current/cookbook/bundles/extension.html}
 *
 * @category {{ projectName }}
 * @package PresentationBundle
 * @subpackage DependencyInjection
 */
class {{ projectName }}PresentationBundleExtension extends Extension
{
    const CONFIG_DIR = __DIR__ . '/../Resources/config/';

    /**
     * Loads a specific configuration.
     *
     * @param array            $configs   An array of configuration values
     * @param ContainerBuilder $container A ContainerBuilder instance
     *
     * @throws \Exception
     */
    public function load(array $configs, ContainerBuilder $container)
    {
        $configuration = new Configuration();
        $this->processConfiguration($configuration, $configs);

        $loaderPath = realpath(self::CONFIG_DIR . 'application');
        $loader = new Loader\YamlFileLoader($container, new FileLocator($loaderPath));
{% for entity_name,Vo in entities %}
        $loader->load('{{ entity_name|lower }}_command.yml');
        $loader->load('{{ entity_name|lower }}_query.yml');
{% endfor %}

        $loaderPath = realpath(self::CONFIG_DIR . 'controller');
        $loader = new Loader\YamlFileLoader($container, new FileLocator($loaderPath));
{% for entity_name,Vo in entities %}
        $loader->load('{{ entity_name|lower }}_command.yml');
        $loader->load('{{ entity_name|lower }}_query.yml');
{% endfor %}
        $loader->load('multiTenant.yml');
        $loader->load('swagger.yml');

        $loaderPath = realpath(self::CONFIG_DIR . 'file/multiTenant');
        $loader = new Loader\YamlFileLoader($container, new FileLocator($loaderPath));
{% for entity_name,Vo in entities %}
        $loader->load('{{ entity_name|lower }}_multiTenant.yml');
{% endfor %}

        $loaderPath = realpath(self::CONFIG_DIR . 'route');
        $loader = new Loader\YamlFileLoader($container, new FileLocator($loaderPath));
{% for entity_name,Vo in entities %}
        $loader->load('{{ entity_name|lower }}_command.yml');
        $loader->load('{{ entity_name|lower }}_query.yml');
{% endfor %}
        $loader->load('multiTenant.yml');
        $loader->load('swagger.yml');
    }
}
